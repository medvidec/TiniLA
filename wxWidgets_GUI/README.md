# GUI in wxWidgets
GUI was redesigned in June 2026 however still using wxWidgets version 2.8.12.

Compilation confirmed in MinGW 5.1.6 on Windows 10.
 * mingw32-make -f Makefile.gcc

## ToDo:
 * cleanup - remove Config-Port menu as d2xx_open uses the usb description
 * Print waveform
 